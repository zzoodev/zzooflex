import styled from "styled-components";
import { AnimatePresence, motion } from "framer-motion";

const LatestSlider = styled.div``;
function Latest() {
  return <LatestSlider></LatestSlider>;
}

export default LatestSlider;
